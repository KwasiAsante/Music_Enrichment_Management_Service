# Dev/test-only — not needed at runtime, so kept out of requirements.txt
# (and therefore out of the Docker image). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
